# multi-agent-infra

A cloud-hosted, always-on **Debian agent box** that runs **multiple opencode
sessions, one Unix user per client**: a small Google Compute Engine VM running
Debian 12, reached over Tailscale from your laptop or phone. Long-running agents
(via tmux, or `opencode serve`) keep working while your local machine is asleep.
Infrastructure is defined as code (Terraform), with a single source of truth for
configuration shared between the bootstrap script and Terraform.

**This README is just the front door.** What to read depends on the question:

| Question | Where |
|---|---|
| What can it do, and how do I reach it (command ↔ script ↔ flow) | [`docs/capabilities.md`](docs/capabilities.md) |
| How to operate it day to day (config, browsers, phone, teardown, gotchas) | [`docs/operating.md`](docs/operating.md) |
| What is this for, what exists, what is undecided | [`SPEC.md`](SPEC.md) |
| What is still to be done | [`TASK.md`](TASK.md) |
| **Why** something is the way it is | [`docs/decisions/`](docs/decisions/README.md) |
| The numbers behind those decisions | [`docs/measurements.md`](docs/measurements.md) |

---

## Architecture

```
┌─────────────────────────────────────────────────────────────┐
│  GCE VM  "cloud-agent"   (Debian 12, me-west1)              │
│                                                             │
│   • Debian stable + apt; unattended security updates        │
│   • tailscaled (systemd) -> joins tailnet as "cloud-agent"  │
│   • Tailscale SSH: `ssh <user>@cloud-agent` lands here      │
│   • opencode (pinned) + Node 24 — the agent runner, one user per client
│   • Xvfb :99 + headed-chromium (CDP, persistent profile)    │
│   • x11vnc, loopback only, started by hand (`./run browser`)│
│   • Persistent data disk mounted at /mnt/data               │
│     (writable root — no COS quirks)                         │
└─────────────────────────────────────────────────────────────┘
          ▲
          │  Tailscale (private tailnet, no public inbound)
          ▼
   Laptop / phone:  ssh <user>@cloud-agent
```

---

## The one command

```sh
./run up
```

`up` is a **convergence** command, not a build script: every step first asks what
is already true and does nothing if the answer is "already correct". It ends by
running `verify.sh`, so it can't lie to you about the result. It is
**non-destructive by contract**. Repair a broken box with `up`; run `./run` with
no arguments to list everything.

The full command surface, every script, and the flows they compose are mapped in
[`docs/capabilities.md`](docs/capabilities.md); the detail is in
[`docs/operating.md`](docs/operating.md).

---

## Repository layout

```
multi-agent-infra/
├── README.md             # this file: the front door
├── SPEC.md               # goals, what exists, open questions
├── TASK.md               # open work
├── run                   # operational entry point — `./run`, `./run rebuild`
│
├── config.env            # SINGLE SOURCE OF TRUTH (git-ignored; holds secrets)
├── example.config.env    # template to copy
├── .gitignore
│
├── terraform/            # all Terraform lives here (`terraform -chdir=terraform`)
│   ├── versions.tf       # provider requirements + provider config
│   ├── compute.tf        # Debian image, persistent data disk, the VM itself
│   ├── network.tf        # dedicated VPC + locked-down firewall (no public inbound)
│   ├── startup.tf        # two-phase boot: reach the box fast, then install the rest
│   ├── variables.tf      # input variables (defaults + docs)
│   ├── outputs.tf        # instance_ip + phone settings consumed by the scripts
│   ├── .terraform.lock.hcl        # committed: pins the provider version
│   ├── backend.tf                 # GENERATED by bootstrap.sh — GCS state backend
│   └── tailscale.auto.tfvars      # GENERATED by bootstrap.sh — minted one-off auth key
│
├── scripts/              # everything `./run` dispatches; mapped in docs/capabilities.md
│   ├── lib.sh            # shared: repo-root resolution, config, ssh/gcloud helpers
│   ├── up.sh             # THE command: converge everything to verified, non-destructively
│   ├── bootstrap.sh      # state bucket, backend.tf, terraform init, mint auth key
│   ├── cleanup.sh        # tear down compute + bucket + local files + tailnet node
│   ├── tailscale-api.sh  # mint/validate one-off auth keys, delete stale tailnet nodes
│   ├── rekey.sh          # get a working auth key into an EXISTING VM (apply can't)
│   ├── wait-ready.sh     # block until the VM is on the tailnet and provisioned
│   ├── provision-phone.sh# phone-side parser, boot survival, re-key to current VM key
│   ├── verify.sh         # asserts the whole end state; non-zero exit on any drift
│   ├── verify-browser.sh # browser-stack sidecar; verify.sh runs it concurrently
│   ├── browser.sh        # `./run browser`: hand-drive the box's browser over VNC
│   ├── measure-resources.py  # `./run measure`: PSS by user/kind, peaks, MemAvailable
│   ├── lsp-probe.mjs     # `./run lsp-probe`: what one language server costs on a repo
│   └── drive-agent*.mjs / agent-stress*.mjs / box-*.sh
│                         # measurement toolchain — see docs/capabilities.md
│
└── docs/
    ├── capabilities.md   # command ↔ script ↔ flow map
    ├── operating.md      # the operator's manual
    ├── decisions/        # every settled question + status (see decisions/README.md)
    └── measurements.md   # the evidence those decisions rest on
```

Every script resolves the repo root from its own path and `cd`s there, so they can
be run from anywhere; `run` is a thin, ordered dispatcher over them. Terraform is
never run by `cd`-ing — `scripts/lib.sh` wraps it as `terraform -chdir=terraform`.

---

## Setup

```sh
cp example.config.env config.env   # then edit the values
./run up
```

From an empty project that does the whole thing: state bucket → `backend.tf` →
`terraform init` → one-off auth key → VM + data disk → wait for first boot →
provision the phone → verify. `docs/operating.md` has the config reference, the
prerequisites, and the day-to-day commands (connect, browsers, phone
notifications, teardown).

## Teardown

```sh
./run cleanup                # FULL wipe (VM + data disk + state bucket + tailnet node)
./run tf destroy -target=google_compute_instance.agent && ./run up   # replace the VM, keep data
```
